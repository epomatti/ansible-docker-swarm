docker service create --name helloworld alpine ping docker.com

docker service create --mode global --name helloworld alpine ping docker.com

docker service ps helloworld


docker service create \
--replicas 2 \
--name nginx \
--network services \
--publish 80:80 \
nginx